# RKS Property Intelligence - Backend Environment Configuration

PORT=5000
NODE_ENV=development
JWT_SECRET=rks_property_intelligence_super_secret_jwt_key_2026
CLIENT_URL=http://localhost:5173

# PostgreSQL Database Connection String (Optional for Cloud DB e.g., Neon/Render; defaults to embedded PGlite if omitted)
# DATABASE_URL=postgres://postgres:password@localhost:5432/rks_db

# Supabase Storage Credentials (Optional for Cloud Storage; defaults to local /uploads/ if omitted)
# SUPABASE_URL=https://your-supabase-project.supabase.co
# SUPABASE_ANON_KEY=your-supabase-anon-key
